# figures — 08_finetune_fireprot

- **`01_finetune_bars.png`** — pooled Pearson r, A (Tsuboyama-only) vs D (fine-tuned),
  across identity thresholds. Left: FireProt-test (D > A at 30/50 %; 90 % dips in
  Pearson though Spearman still rises — see results.csv). Right: Tsuboyama-test
  (essentially unchanged → no catastrophic forgetting).
