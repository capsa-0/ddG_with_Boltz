# figures — 02_stress_extrapolation

- **`01_extrapolation_pred_vs_actual.png`** — predicted vs measured ΔΔG for the
  in-distribution baseline (held-out mild, |ΔΔG|<1) and the extrapolation tail
  (ΔΔG>2), with y=x and fit lines. The tail predictions are a flat cloud capped near
  the top of the training range (fit slope ≈ 0.02) — the model does not extrapolate.
